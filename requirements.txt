# Core analysis engine (rules, features, adapters) needs only numpy.
numpy>=1.24

# Pose estimation from real video needs these. They are imported lazily, so the
# engine and its tests run without them installed.
# mediapipe capped at <0.10.30: the legacy `mp.solutions` API (Pose etc.) the
# estimator relies on was removed in 0.10.30. 0.10.21 is the last release with it.
mediapipe>=0.10.14,<0.10.30
opencv-python>=4.8

# Dev
pytest>=7.4
