# Core analysis engine (rules, features, adapters) needs only numpy.
numpy>=1.24

# Pose estimation from real video needs these. They are imported lazily, so the
# engine and its tests run without them installed.
# mediapipe capped below 1.0: the 1.0 release removed the legacy `mp.solutions`
# API (Pose etc.) the estimator relies on; 0.10.x still ships it.
mediapipe>=0.10.14,<1.0
opencv-python>=4.8

# Dev
pytest>=7.4
